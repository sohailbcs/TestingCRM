<!-- Your existing HTML code... -->
<!-- Bootstrap CSS -->
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">

<!-- jQuery -->
<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>

<!-- Bootstrap JS -->
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
<!-- partial -->
<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-md-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <p class="card-title"> Our Clup Members  </p>
                        <div class="row">
                            <div class="col-12">
                                <div class="table-responsive">
                                    <table id="brandTable" class="display expandable-table" style="width:100%">
                                        <thead class="thead-dark">
                                            <tr>
                                                <th>First Name</th>
                                                <th>Last Name</th>
                                                <th>Email</th>
                                                <th>Contact Number</th>
                                               
                                            </tr>
                                        </thead>
                                        <tbody>
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>


            </div>
        </div>

    </div>

</div>
<!-- main-panel ends -->

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#brandTable').DataTable({
                "ajax": {
                    "url": "/Admin/ClubMember/GetService", // Replace with your actual endpoint for retrieving data
                    "type": "GET",
                    "datatype": "json"
                },
                "columns": [
                    { "data": "fristName", "autoWidth": true },
                    { "data": "lastName", "autoWidth": true },
                    { "data": "email", "autoWidth": true },
                    { "data": "contactNumber", "autoWidth": true },
                     
                ]
            });

            // Delete button click event
            $('#brandTable tbody').on('click', '.delete-btn', function () {
                var brandId = $(this).data('id');
                // Set the data-id attribute of the confirmDeleteBtn
                $('#confirmDeleteBtn').attr('data-id', brandId);
                // Show the delete confirmation modal
                $('#deleteModal').modal('show');
            });

            // Confirm delete button click event
            $('#confirmDeleteBtn').on('click', function () {
                var brandId = $(this).data('id');
                // Perform AJAX delete request
                $.ajax({
                    url: '/Admin/Brand/Delete/' + brandId, // Replace with your actual endpoint for deleting data
                    type: 'POST', // Change to 'DELETE' if your server supports it
                    success: function () {
                        // Reload the DataTable after successful deletion
                        $('#brandTable').DataTable().ajax.reload();
                    },
                    error: function (error) {
                        console.error('Error deleting record:', error);
                    }
                });
                // Hide the delete confirmation modal
                $('#deleteModal').modal('hide');
            });
            $('.dataTables_length').hide(); // Hide the 'Show entries' dropdown
        });
    </script>
}
